{
  "name": "amtrak-find-trains-e2e",
  "version": "1.0.0",
  "description": "Playwright + TypeScript E2E suite for the amtrak.com \"Find trains\" homepage search form (Senior SDET take-home).",
  "type": "commonjs",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test --project=mocked-chromium",
    "test:mocked": "playwright test --project=mocked-chromium --project=mocked-mobile",
    "test:mobile": "playwright test --project=mocked-mobile",
    "test:live": "playwright test --project=live-chromium",
    "test:all": "playwright test",
    "test:smoke": "playwright test --project=mocked-chromium --grep @smoke",
    "test:headed": "playwright test --project=mocked-chromium --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --project=mocked-chromium --debug",
    "report": "playwright show-report",
    "codegen": "playwright codegen https://www.amtrak.com/home",
    "install:browsers": "playwright install --with-deps chromium",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "check": "npm run lint && npm run typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.5",
    "eslint": "^9.12.0",
    "husky": "^9.1.7",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.8.1"
  }
}
